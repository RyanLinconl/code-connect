{
  "name": "code-connect",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "npm run prisma:generate && npm run prisma:migrate && next build",
    "start": "next start",
    "lint": "next lint",
    "postinstall": "npm run prisma:generate && npm run prisma:migrate && npm run seed",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate deploy",
    "seed": "node prisma/seed.js"
  },
  "dependencies": {
    "@prisma/client": "^5.9.1",
    "next": "^14.2.28",
    "prisma": "^5.22.0",
    "react": "^18",
    "react-dom": "^18",
    "remark": "^15.0.1",
    "remark-html": "^16.0.1",
    "sharp": "^0.34.1",
    "winston": "^3.11.0"
  }
}
